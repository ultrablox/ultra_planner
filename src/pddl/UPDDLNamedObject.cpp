
#include "UPDDLNamedObject.h"

UPDDLNamedObject::UPDDLNamedObject(const std::string & _name)
    :name(_name)
{
    //memset(name, 0, sizeof(name));
    //memcpy(name, _name, strlen(_name));
}
